inherit ITEM;

void create()
{
        set_name("特制竹筒", ({ "special can","special","can" }) );
        set("unit", "支");
        set("long", "平凡无奇的竹筒，但你却不知道要如何开启，里面似乎有存放东西\n");
        set_weight(100);
        set("value",0);
}

