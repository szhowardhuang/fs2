deep_inherit_list(3)  LPC 系统函数  deep_inherit_list(3)

名称
     deep_inherit_list() - 找出物件的所有祖先(递回式)

语法
     string *deep_inherit_list( object obj );

描述
     传回物件 <obj> 的所有祖先的档名.
     会递回式的往前找出所有祖先.

请参考
     inherit_list(3), inherits(3)

MudOS                Last change: 5 Sep 1994                    1

