crypt(3)              LPC 系统函数              crypt(3)

名称
     crypt() - 把一个字串加密保护

语法
     string crypt( string str, string seed );

描述
     把 <str> 以 <seed> 的前两字元当种子加密保护, 假如没给 seed,
     则会随机取两个字元当种子.
     传回值的前两个字元就是种子本身.
     加密方式跟一般的 UNIX 的方式一样.

MudOS                Last change: 5 Sep 1994                    1

