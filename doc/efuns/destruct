destruct(3)           LPC 系统函数           destruct(3)

名称
     destruct() - 把某物件从记忆体中除去

语法
     void destruct( object ob );

描述
     把物件 <ob> 从记忆体中除去.
     注意, 如果 <ob> == this_object(), 程式不会因此而中止执行,
           不过, 这样也许会造成混淆, 因此劝您在此状况下用本函
           数的话, 能立刻返回.

请参考
     clone_object(3), new(3), destruct_env_of(4), move(4)

MudOS                Last change: 5 Sep 1994                    1

